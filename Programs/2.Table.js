// Table Printing

const tab = 28;

for (let i = 1; i <= 10; i = i + 1) {
  // console.log(tab * i);

  // String Concatination
  const finalOutput = tab + 'x' + i + '=' + tab * i;
  console.log(finalOutput);
}
